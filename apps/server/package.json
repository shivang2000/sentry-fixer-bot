{
  "name": "server",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsdown",
    "check-types": "tsc -b",
    "compile": "bun build --compile --minify --sourcemap --bytecode ./src/index.ts --outfile server",
    "dev": "bun run --hot src/index.ts",
    "start": "bun run dist/index.mjs"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.1047.0",
    "@hono/trpc-server": "^0.4.0",
    "@sentry-fixer-bot/api": "workspace:*",
    "@sentry-fixer-bot/auth": "workspace:*",
    "@sentry-fixer-bot/db": "workspace:*",
    "@sentry-fixer-bot/env": "workspace:*",
    "@trpc/server": "catalog:",
    "better-auth": "catalog:",
    "dotenv": "catalog:",
    "drizzle-orm": "^0.45.1",
    "evlog": "^2.14.1",
    "hono": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@sentry-fixer-bot/config": "workspace:*",
    "@types/bun": "catalog:",
    "tsdown": "^0.21.9",
    "typescript": "catalog:"
  }
}
